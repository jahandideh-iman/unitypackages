This package contains third-party software components governed by the license(s) indicated below:

---------

Component Name: Unity Logs Viewer ("Reporter")

Location: `Runtime/ThirdParties/Unity-Logs-Viewer/` and
`Editor/Scripts/ThirdParties/Unity-Logs-Viewer/`

License Type: **UNRESOLVED — do not publish this package until this entry is completed.**

The bundled sources carry no copyright header, no license file and no author attribution. The Unity
import metadata for `Runtime/ThirdParties/Unity-Logs-Viewer/Reporter/Reporter.cs` records
`licenseType: Store`, indicating the files were imported from the Unity Asset Store rather than
obtained from a source distribution.

Redistributing Unity Asset Store content inside a package published under the MIT license is not
permitted by the Asset Store EULA unless the component's own license explicitly allows it. Before
this package is tagged for release, one of the following must happen:

1. The component's upstream license is identified and its full text reproduced here; or
2. The component is removed from the package.

No runtime or editor code in `Runtime/Scripts/` or `Editor/Scripts/` outside the `ThirdParties`
folders references the Reporter types, so removing the component does not break the console itself.
